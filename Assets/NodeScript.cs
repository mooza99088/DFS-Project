using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NodeScript : MonoBehaviour
{
    //variable for tracking the parent of this node

    //a list of Node variables representing all neighbors for this Node

    //a boolean variable for checking if this node has been searched


    // Start is called before the first frame update
    void Start()
    {
        //find all neighboring nodes and add them to neighbor
    }

    // Update is called once per frame
    void Update()
    {
        
    }


    //method for toggling 'searched' between true and false

    // method for settling/clearing the parent node
}
